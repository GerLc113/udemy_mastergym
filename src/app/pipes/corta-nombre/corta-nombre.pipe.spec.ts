import { CortaNombrePipe } from './corta-nombre.pipe';

describe('CortaNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new CortaNombrePipe();
    expect(pipe).toBeTruthy();
  });
});
